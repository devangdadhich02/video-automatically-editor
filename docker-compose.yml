services:
  video-editor:
    build: .
    container_name: ai-video-editor
    ports:
      - "5508:5508"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    restart: unless-stopped

